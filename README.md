# Chat WebSocket Backend

A simple chat application using WebSocket protocol.

## Installation

* Clone the repository
* Run `npm install` to install dependencies

## Usage

* Run `node server.js` to start the server
* Open multiple browser tabs and go to `http://localhost:8080`
* Type a message in any of the tabs and it will be broadcasted to all other tabs
